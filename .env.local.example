# yogico 공유 클러스터 (vercelData·imgCreate 등과 같은 클러스터, DB 만 분리)
MONGODB_URI=mongodb+srv://<user>:<password>@yogico.dmz6oro.mongodb.net/?appName=yogico
MONGODB_DB=cdapi

# 배송 기록 보관 기간(일). 출하일이 있는 건만 대상 — 미출고 건(재고소진 등)은 파일에 있는 동안 보존된다.
RETENTION_DAYS=60
